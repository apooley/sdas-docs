---
title: SDAS CSV Importer
description: User guide for the SDAS CSV Importer plugin, covering inventory feed configuration, background processing, and image optimization.
---

## Overview

The SDAS CSV Importer is a robust tool designed to automate the ingestion of vehicle inventory data from external CSV feeds. It handles the entire lifecycle of an import, from fetching the remote file and parsing its contents to managing background processing and optimizing vehicle images for the web.

## Admin Interface and Configuration

The importer is managed through two primary screens located under the **SDAS** menu in the WordPress admin.

### Import Settings

The **Import Settings** screen (`SDAS -> Import Settings`) is where you configure the core behavior of the inventory synchronization:

-   **Inventory Feed**: Provide the URL to your remote CSV inventory feed. You can also define a list of **Allowed Feed Domains** to enhance security.
-   **Import Schedule**: Choose how frequently the importer should check for updates (e.g., Hourly, Twice Daily, Daily).
-   **Missing Vehicle Action**: Define what happens to vehicles that are present in your WordPress site but missing from the latest CSV feed. Options include:
    -   **Do Nothing**: Keep the vehicle as is.
    -   **Mark as Draft**: Unpublish the vehicle but keep the data.
    -   **Delete**: Permanently remove the vehicle from the site.
-   **Performance Tuning**: Adjust the **Chunk Size** (number of vehicles processed per background batch) to balance import speed with server stability.
-   **Notifications**: Set a **Notification Email** to receive alerts if an import fails or encounters significant errors.

### Image Handling and Optimization

The importer includes sophisticated image processing capabilities, configurable via the **Images** tab in settings:

-   **Storage Strategy**: Choose whether to **Download images to the Media Library** (better for SEO and reliability) or store them as **External URLs** (faster initial import).
-   **Stock Image Filtering**: Automatically exclude generic stock images from your galleries by detecting "stock_image" patterns in URLs.
-   **WPCompress Integration**: Enable CDN-based image optimization, allowing you to serve compressed, WebP-formatted images directly from the WPCompress network.
-   **Re-Image Integration**: Connect to the Re-Image API to automatically process and enhance vehicle photos during the import process.
    -   **Cache Warming**: Optionally enable CDN cache warming for Re-Image URLs. You can select specific widths (e.g., 250w, 650w) to prime after uploads, improving performance for common image sizes.
    -   **Clear Pending Warmups**: A utility button to clear any pending cache warmup jobs from the background queue.
    -   **Restore Image Order**: A maintenance tool to align vehicle gallery order with the live CSV feed. It compares vehicles against the feed and restores order for mismatched external/Re-Image galleries. It includes a **Dry run** mode (enabled by default) to preview changes. It never overwrites manually-ordered galleries and never re-uploads images.

## The Import Process

The importer operates as a multi-stage pipeline designed for reliability and performance.

1.  **Fetch & Parse**: The system retrieves the CSV file from the configured URL and parses it into structured data.
2.  **Queueing**: Instead of processing everything at once, the importer adds vehicles to a background queue.
3.  **Background Processing**: Using the WordPress Cron system and Action Scheduler, the importer processes the queue in small "chunks." This prevents server timeouts and ensures the site remains responsive for visitors.
4.  **Data Mapping**: CSV columns are automatically mapped to the SDAS Inventory schema, including complex attributes like mechanical specs, pricing, and features.
5.  **Image Pipeline**: As vehicles are imported, their images are sent to the image processing pipeline (WPCompress or Re-Image) for optimization.
6.  **Cleanup**: After the import completes, the system performs a cleanup of temporary data and executes the "Missing Vehicle Action" if configured.

## Monitoring and Troubleshooting

### Import Logs

The **Import Logs** screen (`SDAS -> Import Logs`) provides a detailed history of all synchronization attempts.

**Status badges in the logs list** now use a richer lifecycle model:

| Badge | Meaning |
| :--- | :--- |
| Completed | Import finished successfully |
| Completed with warnings | Import finished but some vehicles had issues |
| Failed | Import encountered a fatal error |
| Skipped | Import was skipped (e.g., feed unchanged) |
| Running + Processing | Import is actively processing in the background |
| Pending + Queued | Import is queued, waiting for a lock or worker |
| Stale / Unfinished | Import never completed and has no active background work |

-   **Terminate Import**: For active imports, a "Terminate Import" button allows you to safely stop the running process at the next safe checkpoint.
-   **Re-Image column**: The logs table includes a **Re-Image** column showing how many vehicles were queued for Re-Image processing during that import run. A dash (`—`) is shown when no vehicles were dispatched.
-   **Log Levels**: Adjust the verbosity of logging (Error, Warning, Info, Debug) in settings to help diagnose complex issues.

#### View Details modal

Clicking **View Details** on any log entry opens a structured diagnostic modal with the following sections:

- **Summary** — Lifecycle label, DB status, Created / Started / Completed timestamps, human-readable duration, and a plain-English explanation of the import's current state.
- **Issues** — A colour-coded list (error / warning / info) that surfaces actionable problems such as failed vehicles, stale imports, missing Re-Image API key, Action Scheduler failures, and partial-progress saves.
- **Import Results** — Processed, Created, Updated, Failed, and **Skipped duplicate rows** counts.
- **Error Message** — The raw error string if the import failed.
- **Re-Image Dispatch** — A breakdown of Re-Image dispatch counts for the run, with a note that background Re-Image work may continue after the vehicle import completes.
- **Diagnostics** — Activity state, Run ID, Force import flag, retry count, likely cause, and Action Scheduler pending/failed job counts. Useful for diagnosing stale or interrupted imports.
- **Phase Timeline** — Timestamps for each internal import phase.
- **Vehicle Errors** — Up to 20 individual per-vehicle error messages recorded during the run.

### Common Troubleshooting Steps

-   **Verify Cron**: Ensure that WordPress Cron is functioning correctly, as it is required to drive the background import batches.
-   **Check Feed Access**: Confirm that the web server can reach the remote CSV URL and that the domain is in the "Allowed Feed Domains" list.
-   **Monitor Memory**: If imports are failing on large files, try reducing the **Chunk Size** in settings.

## Per-Vehicle Re-Image Status

On any individual vehicle's edit screen, the **Re-Image Processing** meta box shows a **Post-import dispatch** status block when the vehicle has Re-Image dispatch or pending work recorded against it. This block displays:

- The dispatch **status** (e.g., `queued`, `skipped`, `complete`).
- A short **message** describing the outcome.
- The **timestamp** and **import run ID** of the last dispatch event.

This block is visible regardless of whether the Re-Image integration is currently enabled, so you can always inspect the last known dispatch state for a vehicle even after disabling the feature.

## Related Documentation

<Columns cols={2}>

<Card
  title="SDAS Inventory"
  href="/plugins/sdas-inventory"
  icon="layout-grid"
  cta="Manage vehicle data"
/>

<Card
  title="SDAS System Overview"
  href="/architecture/sdas-system-overview"
  icon="layout-dashboard"
  cta="Understand the architecture"
/>

</Columns>
